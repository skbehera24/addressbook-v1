pipeline {
    agent any

    stages {
        stage('git checkout') {
            steps {
                git 'https://github.com/skbehera24/addressbook-v1.git'
            }
        }
       
    }
}
